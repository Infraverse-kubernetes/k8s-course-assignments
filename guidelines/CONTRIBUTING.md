# Contributing Guide – Kubernetes Course Assignments

این فایل راهنمای رسمی **نحوه انجام، ساختاردهی و تحویل تمرین‌های دوره Kubernetes** است.

لطفاً قبل از شروع هر تمرین، این راهنما را به‌طور کامل مطالعه کنید.

## 🎯 هدف این راهنما

- ایجاد یک ساختار یکسان برای تمرین‌ها
- ساده‌سازی بررسی تکالیف
- افزایش کیفیت یادگیری و مستندسازی
- جلوگیری از ابهام در نحوه تحویل تمرین‌ها


## 📝 نحوه انجام و تحویل تمرین‌ها

### روند کلی
1. برای خودتان یک Repository شخصی بسازید
2. تمرین هر جلسه را در پوشه مربوطه انجام دهید
3. خروجی‌ها (YAMLها + توضیح) را Commit کنید
4. لینک Repository خود را در README اصلی دوره اضافه کنید

> ⚠️ تمرین‌ها نباید به‌صورت فایل zip یا ارسال خصوصی تحویل داده شوند.


## 📂 ساختار استاندارد Repository دانشجو

ریپو تمرین‌های شما باید ساختار زیر را داشته باشد:

```text
k8s-assignments/
├── session-01/
│   ├── manifests/
│   │   ├── deployment.yaml
│   │   └── service.yaml
│   └── README.md
│
├── session-02/
│   └── ...
```

### توضیح:

* هر **Session** پوشه جداگانه دارد
* فایل‌های Kubernetes داخل پوشه `manifests/` قرار می‌گیرند
* هر جلسه **حتماً** یک `README.md` دارد

## ✍️ محتوای README هر جلسه

در فایل `README.md` هر جلسه، موارد زیر باید نوشته شود:

* توضیح کوتاه درباره سناریو
* منابعی که Deploy شده‌اند
* دستورات مهمی که اجرا کرده‌اید (`kubectl`)
* چالش‌ها یا مشکلاتی که با آن مواجه شده‌اید

### مثال:

```md
## What was deployed
- Deployment for nginx
- ClusterIP service

## Commands used
kubectl apply -f manifests/
kubectl get pods

## Challenges
- Service selector was incorrect initially
```


## 🧾 قوانین نام‌گذاری (Naming Rules)

* نام ریپو:

```
k8s-assignments
```

* نام پوشه‌ها:
  * فقط حروف کوچک
  * از `-` به‌جای space استفاده شود

* نام فایل‌ها:
  * معنادار و مرتبط با Resource
  * مثال:

    * `deployment.yaml`
    * `configmap.yaml`
    * `secret.yaml`

## ⚠️ قوانین مهم

* مشاهده تمرین‌های دیگران آزاد است، **کپی مستقیم ممنوع**
* تمرین بدون README بررسی نخواهد شد
* در صورت ابهام، سوال خود را مطرح کنید

---

موفق باشید 🚀
یادگیری Kubernetes یک مسیر عملی است، نه حفظی.


